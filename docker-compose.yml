services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: forge-ai:latest
    container_name: forge-ai-web
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
